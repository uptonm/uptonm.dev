node_modules
.turbo
.next
.DS_Store

# local env files (keep .env.example tracked)
.env
.env*.local
.vercel
.env*

# Locally installed agent skill tooling (Neon integration)
.agents/
.claude/skills/neon
.claude/skills/neon-postgres
skills-lock.json

# Local git worktrees (e.g. parallel branch work)
.claude/worktrees/
